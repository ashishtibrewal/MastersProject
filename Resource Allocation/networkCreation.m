function [networkMap, occupiedMap, distanceMap, latencyMap, bandwidthMap] = networkCreation()
% Function to create the data center network

% Note that the latency map should have an almost linear relationship
% with the distance map (i.e. If the distance increase, the latency has to
% increase)

% Adjacency matrix

% Assumptions on inter-blade (i.e. intra-blade) need to be made

networkMap = 0;
occupiedMap = 0;
distanceMap = 0;
latencyMap = 0;
bandwidthMap = 0;

end